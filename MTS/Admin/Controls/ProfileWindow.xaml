<Window x:Class="MTS.Admin.Controls.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MTS.Admin.Controls"
        Title="Operator profile" Height="260" Width="300" Icon="/MTS;component/Resources/icons/profile.png">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Content="Name:"/>
            <Label Grid.Column="1" x:Name="nameLabel"/>
            
            <Label Grid.Row="1" Content="Surname:"/>
            <Label Grid.Row="1" Grid.Column="1" x:Name="surnameLabel"/>
            
            <Label Grid.Row="2" Content="Group:"/>
            <Label Grid.Row="2" Grid.Column="1" x:Name="groupLabel"/>
        </Grid>
        <Label Content="Change password:" FontSize="14"/>
        <local:ChangePasswordControl x:Name="changePassword" Margin="10"
           PasswordFailed="changePassword_PasswordFailed" PasswordChanged="changePassword_PasswordChanged"/>
        <Label x:Name="errorLabel" Foreground="Red"/>
    </StackPanel>
</Window>
