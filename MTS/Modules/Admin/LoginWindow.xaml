<Window x:Class="MTS.Admin.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login Window" MinHeight="120" MinWidth="200" ShowInTaskbar="false" Width="200" Height="120"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0" Content="Login:"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="loginBox" Height="20" TabIndex="0" />
        
        <Label Grid.Row="1" Grid.Column="0" Content="Password:"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="passwordBox" Height="20" TabIndex="1"/>

        <Button Grid.Row="2" Grid.Column="1" Content="Login" HorizontalAlignment="Right" VerticalAlignment="Bottom" 
                Margin="5"  MinWidth="60" x:Name="login" Click="login_Click" IsDefault="True"/>
    </Grid>
</Window>
