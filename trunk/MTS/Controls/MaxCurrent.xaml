<UserControl x:Class="MTS.Controls.MaxCurrent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:MTS.Controls"
             BorderBrush="Black" BorderThickness="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock>Current:</TextBlock>
        <CheckBox Grid.Column="1" x:Name="enabled">Enabled</CheckBox>

        <TextBlock Grid.Row="1">Max. current:</TextBlock>
        <ctrl:UpDownButton Grid.Row="1" Grid.Column="1" IsEnabled="{Binding ElementName=enabled, Path=IsChecked}"/>

        <TextBlock Grid.Row="2">Overload time:</TextBlock>
        <ctrl:UpDownButton Grid.Row="2" Grid.Column="1" IsEnabled="{Binding ElementName=enabled, Path=IsChecked}"/>
    </Grid>
</UserControl>
