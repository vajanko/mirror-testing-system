<UserControl x:Class="MTS.Controls.IntervalCurrent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:MTS.Controls"
             BorderBrush="Black" BorderThickness="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <TextBlock>Current:</TextBlock>
        <CheckBox Grid.Column="1" x:Name="enabled">Enabled</CheckBox>
        
        <TextBlock Grid.Row="1">Minimum:</TextBlock>
        <ctrl:UpDownButton Grid.Row="1" Grid.Column="1" Decimals="2" MinValue="0" MaxValue="1000" 
                           IsEnabled="{Binding IsChecked, ElementName=enabled}"/>
        
        <TextBlock Grid.Row="2">Maximum:</TextBlock>
        <ctrl:UpDownButton Grid.Row="2" Grid.Column="1" Decimals="2" MinValue="0" MaxValue="1000" 
                           IsEnabled="{Binding IsChecked, ElementName=enabled}"/>
    </Grid>
</UserControl>
