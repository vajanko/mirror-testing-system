<Window x:Class="MTS.Utils.ErrorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="{Binding ErrorTitle, RelativeSource={RelativeSource self}}" MinWidth="220" MinHeight="170"
        Width="200" Height="170" MaxHeight="400" MaxWidth="600" x:Name="root"
        ShowInTaskbar="False">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <Image Grid.Column="0" Grid.Row="0" Margin="20" Stretch="None" x:Name="errorIcon" VerticalAlignment="Top"/>          

        <TextBlock Grid.Column="1" Grid.Row="0" TextWrapping="Wrap" Padding="5" 
                   Text="{Binding Message, ElementName=root}"/>

        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal">
            <Image Width="30" Height="30"/>
            <Image Width="30" Height="30"/>
            <Image Width="30" Height="30"/>
        </StackPanel>
        
        <Button Grid.Column="1" Grid.Row="1" MinWidth="60" MaxWidth="70" Margin="5" VerticalAlignment="Bottom" 
                HorizontalAlignment="Right" IsDefault="True" Click="okButton_Click">OK</Button>
    </Grid>
</Window>
